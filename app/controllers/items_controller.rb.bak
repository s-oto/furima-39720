class ItemsController < ApplicationController
  before_action :authenticate_user!, except: [:index, :show]

   def index
     @items = Item.all
   end

   def new
    @item = Item.new
  end

  def create
    @item = Item.new(item_params)
    binding.pry
    @item = Item.create(item_params)
    redirect_to root_path
    # if @item.valid?
    #   redirect_to root_path
    # else
    #   render :new
    # end
  end

  private
  def item_params
    params.require(:item).permit(:item, :description, :price, :category_id, :status_id, :charge_id, :day_id, :prefecture_id, ,user_id:image)
  end
end
